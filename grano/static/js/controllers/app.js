function AppCtrl($scope, $window, $routeParams, $location) {
    /*identity.session(function(data) {
        $scope.session = data;
    });

    $scope.alert = alertService.get();
    $scope.hideAlert = alertService.hide;

    $scope.$on("$routeChangeStart", function (event, next, current) {
        if (next && next.$$route.accessPolicy==='user') {
            identity.checkSession(function(data) {
                $location.path('/');
                alertService.flash('error', 'Sorry, you need to be logged in to view this page.');
            });
        }
    });*/
}